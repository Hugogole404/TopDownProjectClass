/// @description destruction

//Système de barre de "bonbons"/xp à perfectionner (possible upgrade si elle est maxée)

/*treat += 1;
if (treat >= treatNext)
{
	treat = 0;
	treatNext++;
}*/ 



instance_destroy(other.id); //se détruit après récupération

